import React from "react";
import pulse from "";

const Spinner = () => {
  return (
    <img
      style={{ maxHeight: "60px", maxWidth: "60px" }}
      src={pulse}
      alt="spinner"
    />
  );
};
